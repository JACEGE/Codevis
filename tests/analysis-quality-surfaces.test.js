"use strict";
const test = require("node:test"); const assert = require("node:assert/strict"); const fs = require("node:fs"); const path = require("node:path");
test("quality CLI renders honest language metrics", async () => { const { parseArgs, renderQuality } = await import("../lib/commands/quality.mjs"); assert.equal(parseArgs(["--json"]).json, true); assert.equal(parseArgs(["--baseline", "quality.json", "--max-regression", "1.5"]).maxRegression, 1.5); assert.throws(() => parseArgs(["--baseline", "old.json", "--write-baseline", "new.json"]), /either/); const text = renderQuality({ measured: [{ language: "js", files: 2, parseErrors: 1, resolvedInternalSites: 3, internalSites: 4, internalResolutionPercent: 75 }], note: "not total coverage" }); assert.match(text, /js/); assert.match(text, /75%/); assert.match(text, /not total coverage/); });
test("quality MCP tool is registered for workers", () => { const source = fs.readFileSync(path.resolve(__dirname, "../tools/mcp_server.ts"), "utf8"); assert.match(source, /analysisTools/); assert.match(source, /"analysis_quality"/); });
